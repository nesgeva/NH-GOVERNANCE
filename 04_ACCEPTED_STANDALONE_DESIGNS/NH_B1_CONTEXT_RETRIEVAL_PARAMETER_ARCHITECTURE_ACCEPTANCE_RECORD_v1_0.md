# NH_B1_CONTEXT_RETRIEVAL_PARAMETER_ARCHITECTURE_ACCEPTANCE_RECORD_v1_0.md

**Record type:** Acceptance / closure record — status and provenance only.
This record alters no architecture, corrects nothing in the accepted v1.0
source, creates no new candidate version, integrates nothing into Master
V10, the Design and Wiring Map, Defaults, or cursorrules, authorizes no
implementation, performs no disk/runtime/store action beyond creating this
record, and closes no dependency the accepted package leaves open.

**Plain meaning:** This file is a receipt. It records that Ness accepted
the B1 structure package. It picks no numbers, does not finish Bundle 2,
does not change the main blueprint, and does not start building.

**Date:** July 9, 2026.

**Intended repository placement:** `04_ACCEPTED_STANDALONE_DESIGNS/`
(the physical move into the repository is Ness's action).

**Governing authority order (unchanged):**
1. `01_AUTHORITATIVE/NH_MASTER-20_CORRECTED_v10.md`
   (adopted by Ness June 29, 2026; governing — Master V10 wins if files
   disagree)
2. `01_AUTHORITATIVE/NH_DECISION_DEFAULTS-S19_v2_2.md`
3. `01_AUTHORITATIVE/cursorrules`
4. `01_AUTHORITATIVE/NH_PROJECT_COMPANION_GOVERNANCE_AND_ARCHIVE_v1.md`

Master V10 remains unchanged and continues to govern.

---

## 1. What was accepted

**Ness accepted B1 v1.0.**
`NH_B1_CONTEXT_RETRIEVAL_PARAMETER_ARCHITECTURE_v1_0_CANDIDATE.md` is
**ACCEPTED — as a standalone structural/mechanical architecture package
only**, the accepted B1 package for Bundle 2. It is not Master authority,
not an integrated part of the map, and not a completion declaration for
anything beyond its own package scope.

## 2. Accepted source file

- **Filename:** `NH_B1_CONTEXT_RETRIEVAL_PARAMETER_ARCHITECTURE_v1_0_CANDIDATE.md`
- **GitHub path:** `05_ACTIVE_CANDIDATE/NH_B1_CONTEXT_RETRIEVAL_PARAMETER_ARCHITECTURE_v1_0_CANDIDATE.md`
- **SHA-256:** `da0aa4d22d6bc6554196c15b2c81b5541018a1e01bfbce367d7cf8e623b6a753`
- **Line count:** 393
- **Byte count:** 19,696

The accepted source was read and its identity recalculated on disk
immediately before this record was written: the SHA-256, line count, and
byte count match the expected accepted identity above exactly,
byte-identical.

## 3. Ness's acceptance

**Acceptance date:** July 9, 2026.

**Acceptance (as carried in ChatGPT's prepared instruction):** Ness
explicitly accepts
`NH_B1_CONTEXT_RETRIEVAL_PARAMETER_ARCHITECTURE_v1_0_CANDIDATE.md` as the
accepted B1 structural/mechanical architecture package for Bundle 2.

## 4. What B1 settles (accepted scope)

**B1 settles the safe structure for §7F Context Retrieval settings** —
structure only, no values. The accepted package covers:

- **the two always-separate retrieval channels** — positional and
  semantic — with the settled non-merge, conflict-surfaced,
  semantic-never-overrides-positional rules and channel identity as
  provenance;
- the **per-mode parameter/configuration structure** — versioned,
  append-only mode configuration records with identity, A4 binding by
  reference, bounded-quantity slots, scope and safety fields, and the
  fail-closed validity rule;
- the **per-run audit/log record structure** — one operation, one
  append-only record with exact parameters, versions, items with
  provenance, cuts, enforcement events, and the honest empty-vs-failure
  marker, making hidden retrieval behavior structurally detectable;
- the **safety-ceiling mechanism** — versioned ceiling sets above mode
  settings, minimum-precedence, configuration-time rejection, runtime
  defense-in-depth, and governed versioned change only, so no mode can
  silently expand retrieval beyond its allowed boundary;
- the **A4 consumption-by-reference rule** — no declaration content
  written; no valid declaration, no mode, no retrieval;
- the **B1/B26 seam** — healthy-run structure and honest empty results
  here; the entire system-failure path there.

## 5. What B1 does not do (accepted limits)

- **B1 chooses no empirical values.**
- **B1 chooses no limits, thresholds, budgets, time spans, ceiling
  values, or tuning numbers** — every one is an open slot, tested against
  the gold sets before being locked, later.
- **B1 does not create gold-set results.**
- **B1 does not design B26 failure mechanics** — no index failure,
  stale/incomplete index, timeout, unreachable service, retry,
  stop-vs-degraded behavior, crash recovery, or duplicate prevention.
- **B1 does not perform B-INT-1 wiring.**
- **B1 does not write actual A4 component declarations.**

## 6. Still open — none silently closed

- **Bundle 2 is not complete.**
- **B26 and B-INT-1 remain open**, along with every empirical value, the
  per-component A4 declarations (reserved to Ness), and Bundle 2's
  remaining sequence.
- **Master / map / Defaults / cursorrules integration remains pending**
  for B1 and for every accepted standalone package.
- **No implementation, Cursor work, coding, migration, production store,
  model change, runtime, UI, tuning, or live-disk action is authorized.**

## 7. Files unmodified; nothing reopened

**No authority file was touched.** Master V10, the Design and Wiring Map,
Defaults, cursorrules, and the Companion remain unmodified. **No accepted
package is reopened** — A4 (v1.1 and its acceptance record), Bundle 1's
complete accepted set, A2, and the A1 blocker record all stand exactly as
accepted; no historical file was moved, overwritten, or deleted. The
accepted B1 source itself was not rewritten or modified and is preserved
byte-identically (§2 verification). B1 is **not** marked authoritative.

## 8. Frozen accepted bytes

v1.0 itself must remain byte-identical and retains its historically frozen
CANDIDATE status wording (text frozen at creation time, before audit and
acceptance). The governing current status — **accepted B1
structural/mechanical package for Bundle 2, standalone scope only** — is
established by Ness's acceptance recorded in this separate record, exactly
as Ness's later explicit acceptance governs over stale file-internal
wording.

## 9. Future work path

- Empirical values (limits, thresholds, budgets, spans, ceilings) are
  chosen later through the governed tuning path, tested against the gold
  sets before locking.
- B26 and B-INT-1 proceed as their own packages per the verified plan,
  each under its own exact instruction.
- Any future integration of B1 into Master V10, the map, Defaults, or
  cursorrules requires a separate versioned consolidation candidate, an
  independent audit, and Ness's explicit acceptance — never an in-place
  edit of v1.0 or this record.

## 10. Closure and next work

B1 is closed for its accepted standalone structural scope. Bundle 2
continues — open — according to the accepted eight-bundle dependency
plan. This record begins no next item, reopens nothing, and alters no
plan.

---

*Status and provenance record only, for
`NH_B1_CONTEXT_RETRIEVAL_PARAMETER_ARCHITECTURE_v1_0_CANDIDATE.md`
(SHA-256
`da0aa4d22d6bc6554196c15b2c81b5541018a1e01bfbce367d7cf8e623b6a753`).
Accepted as the standalone B1 structural/mechanical package for Bundle 2.
No empirical value chosen; B26 and B-INT-1 open; Bundle 2 not complete;
all integration and implementation pending. Creates no architecture;
changes no file; closes no open dependency; authorizes no integration or
implementation.*
