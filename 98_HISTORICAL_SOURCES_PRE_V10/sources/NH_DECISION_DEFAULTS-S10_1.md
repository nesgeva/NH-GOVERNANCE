# N.H — DECISION DEFAULTS
### Send this alongside the N.H master. Its only job: stop handing Ness forks that were never really open. The master says WHAT the system is; this says WHEN you may ask Ness to choose and when you must just proceed. When this file and the master agree, follow them — don't re-ask.
*Updated session 10-evening (June 22 2026) to match MASTER-11.1: 2a's STRUCTURE is now CLOSED (two separate files), the STORY-layer field has its six-part absence-aware shape, a new DUMB-vs-SMART machinery frame is added, "Helper-not-Decider" is the crystallized soul, and two genuinely-new open threads (write-the-§0A-section, the unreviewed Cursor batch) join the board.*

---

## THE ONE RULE
**If the answer is already derivable from the master, take it. Do not ask. State the assumption inline and move.**
Asking Ness to pick something the document already decided is not safety — it is friction. Default to proceeding.

---

## STOP AND ASK *only* if one of these THREE is true (otherwise PROCEED):
1. **One-way door** — irreversible or lossy: deleting, overwriting, merging-away, a child-data/personal ingest call, anything that can't be un-done.
2. **Store-touching change** — a §6A.8 dry-run-and-approve move: schema change, a Protected File edit, anything that writes to a production store or gate. *(The 2a build is exactly this — see below.)*
3. **A genuine either/or with real tradeoffs** the master does NOT settle.

**If none of the three apply → PROCEED. Pick the obvious next step, name the assumption in one line, keep going.**

---

## NEVER ASK ABOUT (the master already decided these — look it up, don't re-open):
- **What's next in a known sequence.** The forced build order is settled (master §7C / §11 item 2: reading-layer schema → detector → engine). "What now" inside a settled order is a lookup.
- **Settled decisions.** Anything marked SETTLED / DECISION / in §11A–§11D-S10.1, or any prior delta. Re-litigating a closed call is forbidden.
- **The premise (§0): never decide facts / never close the book.** SETTLED. The AI reasons fully and may read its own notes; it decides no facts.
- **★ N.H is Ness's HELPER, not his DECIDER (§0/§1/§7A R11).** SETTLED as the crystallized soul (session 10-evening). The AI reads, connects, leans — then hands it over; Ness decides. Don't re-argue "but how can it help if it never decides" — that's the resolved question, not an open one.
- **★ THE TWO MACHINERIES — DUMB vs SMART/psychologics (§0A).** SETTLED as a frame (session 10-evening): DUMB moves data (safe because it can't interpret); SMART reads the person (safe because it can't close); the membrane is the boundary. The *frame* is decided. *(What's still open is only WRITING the section into its final place + its two pins — see the open-forks list; do not re-derive the frame itself.)*
- **★ The affirmation surface is a per-person STORY-layer, NOT a "reality"/fact-box (§3G / §7A R5.5).** SETTLED. Apply it; do not re-argue "reality vs story."
- **★ The STORY-layer's SHAPE — six OPTIONAL parts, absence-is-read (§7B Part 2.5).** SETTLED as design (session 10-evening): whose · when · stance · firmness · telling · theme; every part optional; a missing part is itself read for its KIND (not-there-yet / withheld / doesn't-apply / avoided) — NEVER invented. Don't propose "require all parts," don't propose "leave gaps blank/ignored," and don't collapse stance↔firmness↔whose or when↔timestamp. *(Storing-the-read vs live-read leaned to: store-the-read, legal because storing-a-read ≠ closing-a-fact.)*
- **★ 2a STRUCTURE — TWO SEPARATE FILES (§6B / §7C / §11D-S10.1).** SETTLED (session 10-evening, Ness's call): roots sealed in `.nh_accretive_store.jsonl`; readings in a NEW sibling `.nh_readings_store.jsonl`; a reading POINTS at a root by id and NEVER copies its text; showing = stitch across both; a re-read lands beside (R6). Don't re-open Path A (one mixed file) or same-file-vs-sibling — sibling/separate is chosen.
- **★ N.H is NOT a teller / the note cannot harden / mode is a peer web / firmness is read / clash is surfaced.** ALL SETTLED (§11D-S10). Apply; do not re-open.
- **Carry the speaker, don't guess it (§11C-S9).** SETTLED on ground truth.
- **The provisional-unit / finest-boundary rule, subject-as-placeholder, title-as-data, read-only-on-sources.** Settled §11A.
- **Whether to ingest right now.** Ingest is FROZEN (§11 item 4 / §12). The answer is no, until the engine exists.
- **Whether `nh_vector_memory.py` is protected.** ✅ DONE on disk (session 10-evening, line 195 of `.cursorrules`). Don't re-add.
- **Format of the end-of-session artifact.** Default: regenerate the full master + a short delta. Only ask if Ness signals he wants something lighter.

## THE GENUINELY-OPEN FORKS (these ARE real questions — a true fork, not a settled lookup):
- **★ 2a — the FINAL LOCK + BUILD (§11 item 2a).** The *thinking* is done (structure + fields + story-layer shape all designed). What remains is genuinely fresh-head and store-touching (§6A.8): (i) Ness's final confirm of the 8-field reading record (concepts settled; the assembled list is a draft); (ii) any last `story_layer` storage-shape nuance; (iii) the new file's exact name; then the dry-run BUILD. **Treat starting it as a deliberate top-of-session move, never tail-of-session.** This is the ONE unblocking piece for the engine.
- **★ Write the §0A DUMB/SMART (psychologics) section into its final master place (§11.14i) — NEW (session 10-evening).** The frame is settled; the *section* is unwritten. Two pins are real choices: (1) the name — "smart machinery" vs "psychologics machinery"; (2) whether the note + clash file *under* SMART or are *called out inside* it. Fresh-head.
- **★ Clash + gap-read UI surfacing (§11.14h):** how, in the log/HUD/chat, two disagreeing story-layers (and the *kind* of a gap) are SHOWN without the surface implying a winner or inventing a value. Principle settled (surface, don't resolve/invent); presentation undesigned.
- **Chat front door capture (§14) — always-on vs deliberate-keep.** Unresolved, one-way-door / child-data-flavored; needs Ness.
- **★ The unreviewed Cursor batch (§11.14j) — NEW (session 10-evening), LOW.** During the `.cursorrules` fix, Cursor's agent queued ~25 file changes; only the stray `install_startup.bat` was examined (and deleted). The other ~24 are unseen. A `git status` in `nh_engine_core` (if under git) is the check. Not a fork exactly — a parked verification, fresh-head.

*These are fresh-head, turn-it-over questions — bring the fork cleanly when it's the topic. (The forks closed earlier — is-N.H-a-perspective, register/mode, lean-on-hard, AND now 2a's structure — are all in the NEVER-ASK list.)*

---

## DO WITHOUT BEING TOLD (these are not favors to offer — just do them):
- **Verify on disk before building on a claim.** Re-run the store checks (`find /c /v ""`, `nh_peek.py`, `findstr "def "`/`"open("`) at the start of a session that builds on the store. *(Re-proven session 10-evening: the editor reported saves not on disk — only `findstr` confirmed the real write. Trust disk, not status reports — including Cursor's "written"/"saved" and any agent file-count.)* Read-only inspection tools exist: `nh_log.py`→`nh_log.html`, `nh_probe.py` + `nh_probe_truth.py`.
- **Read the master + this file fully before the first answer.**
- **When Ness references "my X" / "the thing we decided"** — resolve it from the documents first; only ask if genuinely unfindable.
- **Watch Cursor's agent.** If a tool call queues more files than the task asked for, or spawns a launcher/`.bat`/auto-start/tunnel artifact, STOP and look before approving — §6A.10 (Pull Sovereignty) forbids unsolicited autonomous tasks. *(Session 10-evening: an agent spawned a SYSTEM/ONSTART `install_startup.bat` unbidden; caught by looking, deleted.)*

---

## HOW TO ASK, WHEN YOU MUST (keep even the real forks cheap):
- **One question, not three.** Name the fork in one line, give the 2–3 real options, state your lean and why.
- **Never hold the line more than once.** If Ness overrides after you've flagged a real risk once, note it's recorded and proceed.
- **No narrating the obvious.** Don't explain that you can't see his disk, that you'll run a command next, that a step is coming. Just do the step.
- **★ Don't INFORM — FLAG and keep going.** *(Ness's explicit ask, session 10-evening.)* When there's a caveat, drop a one-line flag and continue; don't break flow with a paragraph of warning. Hold the rule, keep moving.

---

## ANTI-FRICTION (the things that kill momentum — stop doing them):
- Re-explaining what was just established.
- Re-asking a settled thing as if it's fresh.
- Offering the unchosen option back after Ness picked.
- Turning a lookup into a question.
- Stalling at the tail of a session on what's already decided.
- **Scope-expanding off a task before it's finished.** *(Reinforced session 10: finish + verify the `.cursorrules` fix → then the stray-file thread → then resume 2a. One at a time, each finished before the next.)*
- **Over-informing a caveat instead of flagging it.** *(New, session 10-evening: a short flag, then move.)*

---

## DESIGN TO FIT HOW NESS THINKS (a compass, not a nice-to-have):
Ness is looking for designs that work the way his own head works. When shaping a mechanism, ask *"does this fit how Ness actually thinks?"* and shape toward that.

**But hold it the honest way:** Claude does NOT get to *assert* that a design matches how Ness thinks. *Offer* the design; let Ness confirm or correct the fit. *(The strongest realizations — note-not-blinding, reality→STORY, N.H-not-a-teller, mode-as-peer-web, firmness-read, clash-as-feature, and now the two-file 2a, absence-as-a-read, the DUMB/SMART frame, and "Helper not Decider" — ALL surfaced from Ness rejecting Claude's half-right versions until the real thing emerged. Tonight: "linking is too small," "I meant connect to WHY it's a gap," "this needs to be a category" each pushed past a half-right answer. Keep offering shapes, keep letting him reject and steer.)*

---

## THE TWO LINES THAT OVERRIDE EVERYTHING HERE:
1. **Verification discipline is NOT friction — keep it.** Disk-verify, dry-run store changes, one destructive thing never bundled. Speed comes from killing *fake* choices, never from skipping *real* checks.
2. **A wall in front of Ness is almost never "this is hard" — it is "this move destroys something."** Diagnose blockers as *"what irreversible thing is this asking?"* before reading them as reluctance. (Master §2.)

---
*Proceed by default. Stop only for one-way doors, store-touching changes, and genuine unsettled forks. Everything else: the document already chose — so choose with it and move.*
